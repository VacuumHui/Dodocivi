name: sdxl_collector
description: A Flutter app to collect SDXL dataset from Civitai.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  path_provider: ^2.1.3
  share_plus: ^7.2.1

flutter:
  uses-material-design: true
